# Confirmed environment: Python 3.12.5
torch==2.2.0
torch_geometric==2.6.1
scikit-learn==1.6.1

# Pin the rest to your environment (e.g., from `pip freeze`)
rdkit
pandas
numpy

# Explanations (explain.py)
shap
matplotlib

# Note: torch / torch_geometric / rdkit may need environment-specific
# wheels (CUDA / PyG matching). Install per your setup if pip alone fails.
